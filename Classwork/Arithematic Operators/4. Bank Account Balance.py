# Program to calculate the remaining balance after withdrawal

# Input the current account balance
balance = float(input("Enter the account balance: "))

# Input the withdrawal amount
withdrawal = float(input("Enter the withdrawal amount: "))

# Calculate the remaining balance
remaining_balance = balance - withdrawal

# Display the remaining balance
print("Remaining Balance =", remaining_balance)